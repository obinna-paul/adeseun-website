// Tailwind v4 plugin — taste-skill's rule: for v4, use @tailwindcss/postcss,
// never the old `tailwindcss` plugin entry.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
